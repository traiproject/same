# Task requires Go tool from Nix
exec same run --ci check-go
stdout 'go version go1.25'

-- same.work.yaml --
version: "1"
root: "."
tools:
  go: go@1.25.4
projects: ["."]

-- same.yaml --
version: "1"
project: test
tasks:
  check-go:
    cmd: ["go", "version"]
    tools: ["go"]
